# +--------------------------- setup the avr dev environment --------------------------+
# |                                     avr_dev_v1                                     |
# +------------------------------------------------------------------------------------+

# set version
export AVR_VER=v1

# set avr home
export AVR_HOME=${HOME}/avr_sdk

# extend PATH 
export PATH=${AVR_HOME}/bin::$PATH


# EOF
